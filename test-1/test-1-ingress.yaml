apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-1-ingress
  namespace: default
spec:
  rules:
  - host: test1.100xsidd.me
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: test-1-service
            port:
              number: 8080
